# 개발 TODO — 에이전트·채팅 세션 작업실

기준: 2026-09-16 사용자 스케치 및 메뉴 포함 간결한 데모 검토 이후. 이 문서가 이전 TODO와 인수인계 문서의 작업 순서보다 우선한다. 미체크 항목은 미완료이며, 데모·공식 문서의 지원 가능성을 실제 연동 완료로 간주하지 않는다.

[문서 인덱스](README.md) · [현재 구현 계약](client-runtime.md) · [공통 개발 가이드](development-guide.md) · [이전 TODO 보관](TODO-history-2026-09-14.md)

2026-09-18 UI 보완: 저장된 프로젝트 우선 복원, 첫 연결 확인의 화면 잠금 분리, 상단 조회 상태·재시도·실패 시 이전 값 유지, 환경 조회 중 프로젝트 전환을 구현했다. [구현·검증 범위](client-runtime.md#조회-지연과-초기-로딩-ui--2026-09-18). 채팅 복원에 필요한 설정·자료·대화 조회 자체의 실패 복구 분리는 후속 범위다.

## 1. 제품 목적과 화면 기준

주 목적은 **에이전트 네이티브 세션과 채팅 세션을 관리하고 작업을 이어가는 것**이다. 생산성은 완료 시간·사람 개입·재작업·성공률·토큰 사용량으로 평가한다. 요구사항·산출물 관리가 세션 관리보다 먼저 커지지 않도록 한다.

- [x] 사용자와 기본 정보 배치 합의: 접이식 왼쪽 메뉴, 낮은 높이의 상단 요약 3칸, 중앙 작업 대화, 하단 에이전트·모델 선택과 입력.
- [x] [작업실 데모](../app/web/static/session-demo.html)와 [상세 사용량 데모](../app/web/static/session-metrics-demo.html) 작성. 예시 데이터이며 실제 실행·설정 저장 없음.
- [ ] 데모 배치를 실제 작업실에 적용한다. 현재 `/`의 세션 UI는 이전 단계 구현이며 최신 데모 배치와 다르다.
- [ ] 상단 왼쪽: AI·세션·상태·컨텍스트 사용률·누적 토큰·실제 모델을 간결한 행으로 표시한다. 기본 4행과 내부 스크롤은 제안 상태이며 실제 배치 검토 후 확정한다. 표시 행 수와 동시 실행 한도는 별개다.
- [ ] 상단 가운데: 계정의 시간별·주간 한도 및 초기화 정보를 요약한다. 제공된 제한 창을 사용하고 임의로 주간 한도라고 표시하지 않는다.
- [ ] 상단 오른쪽: 선택 프로젝트와 연결된 Git·개발 서버 포트·Docker 정보를 요약한다. 관련 없는 머신 전체 정보를 나열하지 않는다.
- [ ] 상세 토큰·사용 내역은 별도 대시보드로 이동한다. 메인 영역을 펼쳐 세로로 커지게 만들지 않는다. 최근 채팅은 에이전트별 3개·마지막 실행 순·완료 포함 기준을 재사용한다.
- [ ] 왼쪽 메뉴의 프로젝트·채팅, 룰북·참고 자료, 에이전트 연결·실행 설정, 문서·기록·사용량 화면을 기존 기능과 연결한다.

완료 기준: 합의된 화면에 실제 데이터를 연결하고, 미지원·미수신·조회 실패를 구분한다. 기본 화면에서 상태 요약과 메시지 입력이 함께 보인다.

## 2. 먼저 할 일 — 에이전트 연결과 데이터 계약 확인

- [x] Codex 모델 목록·계정 한도 읽기 전용 API와 실제 작업실 요약 연결. 설치 버전의 실제 응답, 캐시·실패·계정 데이터 필터링 검증 완료. 실행 모델·컨텍스트·모델 선택 전달은 아래 미완료 항목으로 유지한다. [현재 계약](client-runtime.md#codex-계정-조회--2026-09-16)

- [ ] 설치된 Codex 버전에서 모델 목록·실행 모델·토큰·컨텍스트·계정 한도를 어떤 인터페이스로 얻는지 확인한다. 기존 CLI 실행 유지 또는 App Server 연동의 영향·대안을 비교한다. 공식 API 존재만으로 현재 어댑터에서 사용 가능하다고 판단하지 않는다.
- [ ] Claude 설치·회사 계정이 준비되면 같은 항목을 조사한다. 미설치 상태에서는 계약 테스트와 미지원 표시를 진행하고 개인 결제·계정 연결을 선행 조건으로 두지 않는다.
- [ ] 데이터별 출처·범위(계정/프로젝트/채팅/네이티브 세션/실행), 갱신 시점, 수집 상태와 마지막 확인 시각을 정의한다.
- [ ] 선택한 모델과 실제 실행 모델을 구분한다. 모델 선택 전달·지원 여부·자동 변경을 확인하고, 지원하지 않는 모델을 임의 대체하지 않는다.
- [ ] 상태·사용량 조회가 불필요한 모델 호출이나 작업 실행을 유발하지 않도록 하고, 반복 조회 비용·실패·재연결을 검증한다.

참고: [Codex App Server](https://learn.chatgpt.com/docs/app-server)의 모델·토큰 이벤트·계정 한도, [Claude 상태줄](https://code.claude.com/docs/en/statusline)의 모델·컨텍스트·한도 정보는 연동 후보 근거다(2026-09-16 확인). Claude 상태줄과 현재 비대화형 실행은 동일 수집 경로로 가정하지 않는다. 실행 어댑터 교체와 같은 큰 구조 변경은 [공통 가이드](development-guide.md)의 영향 검토를 거친다.

완료 기준: 각 표시 항목에 실제 응답 예시 또는 미지원 근거가 있고, 기존 세션·권한·취소 동작을 유지할 구현 범위가 명확하다.

## 3. 코어 — 채팅과 네이티브 세션 관리

현재는 작업 이름으로 채팅을 묶고 프로젝트·작업·클라이언트·작업 경로·권한별 네이티브 세션을 연결한다. 새 채팅은 생성 즉시 기존 task_states에 저장된다. 단순 이름 변경은 네이티브 세션을 유지한다. [저장 코드](../app/core/storage.py)와 [실행 코드](../app/core/orchestrator.py)를 기준으로 필요한 부분만 확장한다.

- [ ] 안정적인 채팅 식별자와 보관 정책을 정의한다. 빈 채팅 서버 저장·단순 이름 변경 시 세션 보존은 구현했다. 이름 변경과 대화 합치기를 별도 사용자 동작으로 구분한다.
- [x] 같은 브라우저 탭에서 채팅별 작성 중 입력·에이전트·모델·작업 경로·권한·자료·페이지/스크롤을 보존하고 프로젝트/채팅 전환·새로고침에서 복원한다. 같은 브라우저에서 탭 종료 후 복원과 빈 채팅 서버 저장도 구현했다. 여러 탭의 동시 편집·다른 기기 동기화는 후속 범위다. [복원 계약](client-runtime.md#채팅-작성-상태-복원--2026-09-17)
- [ ] 한 채팅에서 에이전트를 전환할 때 각 네이티브 세션 및 중간 결과를 정확히 연결한다. 연결된 세션 ID와 실제 재개 가능 여부를 구분한다.
- [ ] 실행 중·완료·실패·취소·종료 미확인 상태를 실제 이벤트로 표시한다. 분석/검토 같은 상태를 근거 없이 추정하지 않는다.
- [ ] 신규·재개·새 네이티브 세션 시작·취소·실패 후 재시작을 검증한다. 기존 프로젝트당 실행 제한을 유지하며 여러 세션 동시 실행은 별도 요구·격리 검토 후 결정한다.
- [ ] 알림을 저장된 종료 이벤트와 연결해 짧은 실행·재접속 시 누락/중복을 방지한다. 현재 5초 조회 알림은 완전한 알림 기능이 아니다.
- [ ] 실제 전달 프롬프트·응답·세션 ID·작업 경로·적용 규칙·선택 자료·관련 파일 상태의 기록 부족분을 보완한다. 비밀정보를 불필요하게 복제하지 않는다.
- [ ] 하네스 재시작 복원, 원본 CLI 직접 재개, DB 백업·복원을 각각 검증한다. 데스크톱에 기록이 보이는지는 별도 확인한다.
- [ ] 요청 재전송·재시작·재개에서 중복 실행을 방지한다. 프로젝트별 실행 제한을 요청 멱등성으로 간주하지 않는다.

완료 기준: 새 채팅→에이전트 실행→다른 채팅 전환→복귀→재개 흐름에서 기록·입력·실행 대상이 뒤섞이지 않는다.

## 4. 실제 상태 정보 연결

| 항목 | 현재 기반 | 남은 구현 |
| --- | --- | --- |
| 에이전트 | 선택·CLI 설치/버전 확인·실행 상태 | 최신 UI 연결, 연결 상태 갱신 |
| 언어 모델 | Codex 목록·선택 UI, CLI 모델 전달·요청 모델 기록, Claude init 모델 수신 시 기록 | Codex 실제 실행 모델·컨텍스트 수집, Claude 목록 |
| 토큰 | 성공 결과 usage 저장, 전체 실행 집계 | 실시간 지원 범위·캐시 의미·실패 시 부분 사용량 검증 |
| 컨텍스트 | 미수집 | 현재 점유량·유효 한도·압축 후 갱신 수집 |
| 계정 한도 | Codex 실제 제한 창·사용률·초기화 시각 조회와 요약 연결 | Claude 지원 범위 확인, 최신 데모 배치 연결 |
| Git·포트·Docker | 프로젝트 Git·정확한 Compose 경로 기반 컨테이너/포트 조회와 요약 연결 | 일반 호스트 포트·WSL·비 Compose 매핑, 컨테이너 실검증 |

- [ ] 누적 입력/출력 토큰, 현재 컨텍스트 점유율, 계정 한도를 별도 지표로 유지한다. 누적 토큰을 컨텍스트 %나 계정 한도로 환산하지 않는다.
- [ ] CLI별 usage 의미와 캐시 포함 여부를 확인하고 집계 중복을 방지한다. 미수신과 실제 0을 구분하며 부분 수신 범위를 표시한다.
- [ ] 실제 실행 모델 기준으로 컨텍스트 한도를 표시한다. 고정 200k 같은 데모 값을 제품에 옮기지 않는다.
- [ ] Git 브랜치·변경 상태, 포트·프로세스, Docker 컨테이너·공개 포트의 조회 범위를 정의한다. 도구 미설치·권한 부족·엔진 중지·저장소 아님을 구분한다.
- [ ] 프로젝트와 프로세스/컨테이너의 연결 근거를 정한다. 조회 기능이 컨테이너 시작·중지나 프로세스 종료를 수행하지 않게 한다.

참고: [Git 상태 조회](https://git-scm.com/docs/git-status), [Docker 컨테이너 조회](https://docs.docker.com/reference/cli/docker/container/ls/). 로컬 설치·접근·프로젝트 매핑은 아직 검증 전이다.

완료 기준: 메인 요약과 상세 대시보드가 같은 범위의 실제 기록을 사용하고 수집 실패를 정상 값으로 오인하지 않는다.

## 5. 대화 표시와 기존 제어 기능 연결

채팅 내용의 표현 방식은 수정 필요하다는 사용자 요청이 있다. 콘솔 크기가 정보 정리를 대신하지 않는다. 아래 표시 방식은 제안이며 실제 예시로 검토한다.

- [ ] 요청→진행→결과를 한 묶음으로 보여주고 명령·도구 로그는 접어서 볼 수 있게 한다. 변경·검증·미해결·사용자 판단 항목을 구분한다. 내부 추론을 노출하지 않는다.
- [ ] 메시지 길이·코드·로그에 맞는 읽기 폭, 콘솔 확장, 자동 스크롤 중지·새 출력 안내를 검증한다.
- [ ] 입력부 에이전트·모델·권한·참고 자료가 실제 요청과 일치하도록 연결한다. `/codex`, `/claude`의 명시적 선택 우선순위와 미지원 명령 거절을 유지한다.
- [ ] 기존 RuleLoader의 공통 가이드·프로젝트 규칙 자동 적용과 해시 기록을 유지한다. 메뉴에서 실제 적용 범위를 확인하도록 연결한다.
- [ ] 문서 열기와 참고 파일 첨부를 구분하고 기존 편집·저장·미저장 변경 확인·파일 경계를 유지한다.
- [ ] 질문·가설·방향 변경을 구분하는 지침의 실제 행동을 평가한다. `note/run` 구분 자체가 이 기능을 구현한 것으로 간주하지 않는다.
- [ ] 명시적으로 요청한 스킬·플러그인은 사용 가능/불가/확인 불가를 구분하고 실제 사용 근거만 표시한다.

완료 기준: 한 요청의 진행과 결과를 쉽게 구별하고, 메뉴 조작으로 작성 내용이나 실행 대상을 잃지 않는다.

## 6. 사용자 판단·대기·재개

- [ ] 필수 정보 부족과 승인 대기를 구분하고 질문·영향·대안·추천·근거를 표시한다. 이미 승인한 일상 작업은 반복 승인받지 않는다.
- [ ] 승인 범위를 대상 변경과 기준 코드/설계 버전에 연결한다. 승인 전 해당 변경 적용을 막는 실제 CLI 경로를 검증한다.
- [ ] 승인·거절·추가 정보 입력 후 재개하고, 서버 재시작 후 대기 상태와 결정을 복원한다. 독립적으로 가능한 작업은 계속한다.
- [ ] 목표·제약·확정 결정·기각 사유·미해결 질문을 원문과 연결해 보존하고, 재개 시 현재 파일 상태를 확인한다.

행동 평가: “왜 A로 했어?”는 설명 후 작업 지속, “C도 가능해?”는 검토만, “C로 바꿔”는 계획 갱신, 오류 근거는 재검증. 승인 거절·무응답은 승인으로 취급하지 않는다. 가이드 전달과 실제 차단 보장은 별개다.

## 7. 통합 검증과 구현 순서

권장 순서: **연결·데이터 계약 확인 → 채팅/네이티브 세션 기반 보완 → 간결한 데모 UI의 실제 기능 연결 → 상태 정보·상세 대시보드 → 대기·복구 흐름 검증**. UI와 서버의 독립적인 작업은 함께 진행할 수 있다. 전체 책임 분리 리팩터링을 선행 과제로 만들지 않는다.

- [ ] 기존 API·DB·파일·세션 호환성을 보존하고 변경에 필요한 책임만 점진적으로 분리한다.
- [ ] 새 채팅·전환·재개·모델 변경·취소·미설치·사용량 미수신·CLI 종료·SSE 재접속·서버 재시작을 검증한다.
- [ ] Codex 실제 실행으로 모델·세션·사용량·출력을 대조한다. Claude는 회사 계정 준비 후 동일 흐름을 검증한다.
- [ ] 브라우저에서 메뉴·입력·요약·대화 복원과 좁은 창의 배치를 검증한다.
- [ ] 동일 완료 조건의 작업으로 CLI 단독 대비 시간·개입·재작업·성공률·토큰을 비교한다. 추가 수집·색인·모델 호출 비용도 포함한다.

## 재사용할 구현 기반과 검증 범위

- [x] 프로젝트 생성·외부 폴더 등록·휴지통 복원, 파일 접근 경계.
- [x] SQLite v4 메시지·실행·이벤트·클라이언트 세션 저장 및 이전 데이터 보존.
- [x] Codex 실제 실행·재개·파일 생성 검증 이력. Claude는 어댑터 계약 테스트까지만 확인.
- [x] 백그라운드 실행·취소·시간 제한·프로젝트별 실행 제한, SSE 저장 이벤트 재전송.
- [x] 공통 개발 가이드·프로젝트 규칙 전달과 해시 기록, 목표 유지·자율 진행 지침 보강.
- [x] 기존 문서 편집 모달·참고 파일 선택·설정·프로젝트 관리 기반.
- [x] 세션 중심 1차 UI와 `/api/session-overview`, 채팅·에이전트·네이티브 세션별 토큰 집계 및 최근 3개 조회 표시.

검증 이력: 1차 세션 UI 변경 당시 전체 44 passed / 1 skipped, 이후 세션 현황·사용량 테스트 2 passed. 데모 메뉴·배치는 브라우저에서 확인했다. 이 수치는 이번 TODO 편집에서 다시 실행한 결과가 아니며, 최신 데모의 실제 연동 완료를 의미하지 않는다. 상세 현황은 [실행 계약](client-runtime.md)을 따른다.

## 후속: 첨부 자료와 단계별 산출물

- [ ] Markdown/텍스트부터 자료 등록·파싱 범위·크기·실패 상태를 정하고 PPT·Excel·HTML 등으로 확장한다.
- [ ] 요구사항→설계→검토→구현의 산출물 파일·버전·생성 에이전트·입력 자료·결정을 연결한다. 모든 작업에 고정 단계를 강제하지 않는다.
- [ ] 초안과 확정본을 구분하고 다음 에이전트에 지정 버전을 전달한다. “방금 나온 설계”의 대상이 여러 개면 확인한다.
- [ ] 기록·산출물 보관/삭제 정책을 정한다.

## 향후: 스토리보드 분석과 화면별 요구사항·테스트 조회

2026-09-16 사용자 요청으로 추가한 백로그다. 오늘의 기본 UI·코어 기능 구현 이후에 진행하며, DB 스키마나 분석 도구 선택은 아직 확정하지 않았다. 아래 단계별 산출물 흐름과 연결할 후속 기능이다.

- [ ] 스토리보드 한 페이지를 입력받아 화면 요소, 사용자 행동, 입력·검증 조건, 로딩·빈 결과·성공·실패 상태를 분석한다. 이미지·PDF·PPT의 실제 지원 범위는 구현 시 정한다.
- [ ] 동작의 표시 방식(모달·화면 이동·새 창)과 갱신 방식(전체 새로고침·부분 데이터 재조회·상태 초기화)을 구분한다. 전달값, 저장·취소·닫기 동작, 검색 조건·페이지·스크롤·입력값 유지 여부도 정리한다.
- [ ] 분석 결과를 명시된 사실·추정·확인 필요로 구분하여 사용자가 검토할 수 있게 한다. 원문에 없는 동작을 확정 요구사항으로 저장하지 않는다.
- [ ] 화면 ID → 동작 → 요구사항 ID → 테스트케이스 ID의 관계를 DB에 저장하고 화면번호로 관련 내용을 조회한다. 여러 화면에서 공유하는 요구사항과 여러 요구사항을 검증하는 테스트의 연결도 고려한다. 관계 조회를 위해 별도 그래프 DB 도입을 전제하지 않는다.
- [ ] 원문 파일·페이지/위치, 버전, 초안·확정·폐기 상태를 연결한다. 원문과 DB 중 수정 기준을 정하고, 변경·삭제 시 연결을 갱신하며 영향받은 테스트를 재검토 대상으로 표시한다.
- [ ] 화면별 관련 요구사항 본문·완료 조건·테스트 정의와 필요한 화면 자료를 작업 입력으로 구성한다. ID만 전달하거나 전체 문서를 매번 일괄 주입하지 않는다. 테스트 정의와 실제 테스트 코드·실행 결과를 구분한다.
- [ ] 화면 한 페이지로 추출→사용자 검토→저장→조회→작업 전달을 검증한다. 변경 후 오래된 정의가 사용되지 않는지 확인하고, 초기 추출·갱신 비용까지 포함해 반복 문서 읽기 대비 토큰·시간·누락률을 비교한다.

완료 기준: 화면번호로 해당 버전의 요구사항과 테스트 정의를 원문 근거와 함께 조회하여 작업에 전달할 수 있고, 미확정 항목 및 변경으로 재검토가 필요한 항목을 식별할 수 있다. 실제 구현·테스트 성공은 별도 검증 결과로 판단한다.

## 다음 작업: CLI 연결과 계정 인증

2026-09-16 진행: [API 연결 시나리오](connection-scenarios.md)와 일괄 진단 API를 추가했다. Codex 실제 실행·재개·usage 저장을 확인했고 Claude 인증 필요 상태를 구분했다. 모델 인자 전달·요청 모델 기록·Codex 모델 선택 UI는 연결했으며, 로그인 창 시작·상태 알림 UI·일반 호스트 포트 연결은 아직 남아 있다.

2026-09-17 검증: 별도 CLI 세션의 목록·미리보기·연결 후 실제 재개, 이전 표식 기억, 사용량·응답·이벤트 저장, 앱 재생성 후 재개까지 확인했다. 공통/기존 API 회귀 테스트로 채팅 간 세션 분리와 실패·취소 시 연결 해제를 확인했다. 데스크톱 출처 세션 재개와 브라우저 전환·새로고침 검증은 남아 있다. [검증 범위](client-runtime.md#기존-codex-세션-재개-검증--2026-09-17)

- [ ] Codex·Claude의 설치/경로 설정과 계정 인증 상태를 별도로 확인하고 연결 화면에 함께 표시한다. CLI 설치 확인만으로 연결 완료로 표시하지 않는다.
- [ ] 각 CLI의 로그인 시작·완료 후 재확인 흐름을 구현한다. 인증 정보는 공개 소스와 실행 DB에 복제하지 않는다.
- [ ] 상태에 채워진 원 문자 `●`와 상태명을 적용한다: 빨강 오류, 노랑 주의·로그인 필요, 녹색 정상·인증 확인, 파랑 연결·실행 중, 회색 미설정·미확인.
- 위 항목은 사용자 합의 사항이며 현재 구현 완료 상태가 아니다.

## 보류 기능

플러그인 자동 검색·추천·설치 플랫폼, 팀 공유 서버 인증, worktree별 동시 실행, 그래프 DB 도입, 고급 자동 분류·멀티에이전트 고도화는 실제 필요와 측정 근거가 생길 때 검토한다. 실행 기록 고급 검색도 세션 핵심 흐름 이후다.

### 실행 환경 연동 진행 (2026-09-16)
- [x] Git 브랜치·변경 항목, 정확한 Compose 경로 라벨 기반 컨테이너 상태·포트 조회와 요약 UI 연결.
- [ ] 일반 호스트 개발 서버의 프로젝트별 포트 매핑, WSL/다른 경로 표기 및 비 Compose 컨테이너 매핑.
- 실제 harness API에서 Git main·변경 항목 조회 확인. Docker는 엔진 연결 불가 상태로 확인했으며 실행 중인 컨테이너 실검증은 미완료.

### 2026-09-17 세 가지 E2E 완료 범위

- [x] 실제 Codex 실행 중 채팅 전환, 다른 채팅 초안·에이전트 선택 유지, 복귀/새로고침 후 결과 복원.
- [x] 실제 Codex 취소, 테스트 대역 실패 후 재요청, 격리 서버 강제 재시작 후 확인·정리·재요청.
- [x] 접수 응답 유실 재현 후 새로고침·재전송 시 중복 실행 방지, 요청·진행/결과 구분과 도구 로그 기본 접힘.

전체 81 passed / 1 skipped, JS 10 passed. 실제 CLI와 주입 검증의 구분·재전송 보장 범위는 [실행 계약](client-runtime.md#세-가지-e2e-검증--2026-09-17)을 따른다. Claude 실제 실행과 데스크톱 출처 세션 재개는 미검증이다.
